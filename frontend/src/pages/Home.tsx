// import styled from "@emotion/styled";
import { HeroSection, Button } from "../components/emotion";

const Home = () => {
  return (
    <div>
      <HeroSection>
        <Button primary="blue">Explore Songs</Button>
        <Button>See Stats</Button>
      </HeroSection>
    </div>
  );
};

export default Home;
